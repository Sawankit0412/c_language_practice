/*let's start to find out no of square root.

eg= 5 square in 25;
 */
#include<stdio.h>
int main(){

    int num;
     //square of no = num;
    printf("Enter the no ");
    scanf("%d", &num);

    printf("square of given no %f is",  sqrt(num));
    return 0;
}